import re

def extract_bkt_items(pdf_lines):
    """
    BKT / Tyre Format Dedicated Item Table Parser Logic (Placeholder).
    Will be configured precisely when you are ready to write rules for BKT format.
    """
    parsed_items = []
    
    # BKT format usually has Material Grp, HS Code, Quantity, Value, Gross/Net Weight etc.
    # We will implement this parser precisely as per BKT's PDF layout when requested.
    
    return parsed_items
